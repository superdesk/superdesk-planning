describe('superdesk.planning module', function() {

    beforeEach(angular.mock.module('superdesk.planning'));

    describe('planning settings controller', function() {
        it('should ....', inject(function($controller) {
            var ctrl = $controller('PlanningSettingsController');
            expect(ctrl).toBeDefined();
        }));
    });
});
