const defaultFilterValues = () => (
    {
        name: '',
        calendars: [],
        agendas: [],
    }
);

// eslint-disable-next-line consistent-this
const self = {defaultFilterValues};

export default self;
